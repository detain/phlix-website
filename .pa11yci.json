{
  "defaults": {
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-setuid-sandbox", "--disable-dev-shm-usage"]
    },
    "concurrency": 4,
    "useIncognitoBrowserContext": false,
    "standard": "WCAG2AA",
    "reporters": ["cli"]
  }
}
